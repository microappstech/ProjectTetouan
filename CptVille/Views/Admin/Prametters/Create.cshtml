@using CptVille.Constant;
@model Parameters
@{
    Layout = "_AdminLayout";
}
<main class="main">

    <div class="card">
        <div class="card-header">
            <strong>إنشاء فئة الرئيسية </strong> 
        </div>
        <div class="card-block">
            <form asp-controller="Section" asp-action="Create" method="post" class="form-horizontal ">
                <div class="form-group row">
                    <label asp-for="TypePara" class="control-label col-md-2"></label>
                    <select asp-for="TypePara"
                            class="form-control"
                            asp-items="Html.GetEnumSelectList<TypeParams>()">
                        <option>Select type ...</option>
                    </select>
                    <span asp-validation-for="TypePara" class="text-danger"></span>
                </div>
                <div class="form-group row">
                    <label class="col-md-3 form-control-label" for="hf-email">الإسم الفئة الرئيسية</label>
                    <div class="col-md-7">
                        @Html.TextBoxFor(model => model.Key, new {@placeholder="الإسم الفئة الرئيسية", @class = "form-control"})

                    </div>
                    <button type="submit" class="btn btn-sm btn-primary col-md-2 p-x-3" style="padding-top:8px; padding-bottom:8px;"><i class="fa fa-dot-circle-o"></i> حفظ</button>
                </div>
            </form>
        </div>

    </div>     
</main>